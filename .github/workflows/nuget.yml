name: NuGet Publish

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  publish:
    name: Publish to NuGet
    runs-on: ubuntu-latest
    outputs:
      is_stable_tag: ${{ steps.release-info.outputs.is_stable_tag }}
      release_tag: ${{ steps.release-info.outputs.release_tag }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Required for Source Link to embed full commit history
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - name: Validate tag matches repo version
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            repo_version=$(grep -oPm1 '(?<=<RepoVersion>)[^<]+' Directory.Build.props)
            if [[ -z "$repo_version" ]]; then
              echo "RepoVersion was not found in Directory.Build.props"
              exit 1
            fi

            tag_version="${GITHUB_REF_NAME#v}"
            tag_core_version="${tag_version%%-*}"

            if [[ "$tag_core_version" != "$repo_version" ]]; then
              echo "Tag version '$tag_version' does not match RepoVersion '$repo_version'."
              echo "Expected a tag like 'v$repo_version' or 'v$repo_version-rcN'."
              exit 1
            fi
          fi
      - name: Restore
        run: dotnet restore
      - name: Build
        run: dotnet build --no-restore -c Release
      - name: Determine release metadata
        id: release-info
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            echo "release_tag=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
            if [[ "${GITHUB_REF_NAME}" == *-* ]]; then
              echo "is_stable_tag=false" >> "$GITHUB_OUTPUT"
            else
              echo "is_stable_tag=true" >> "$GITHUB_OUTPUT"
            fi
          else
            echo "release_tag=" >> "$GITHUB_OUTPUT"
            echo "is_stable_tag=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Pack main library
        run: dotnet pack AspNetCore.Identity.CosmosDb/AspNetCore.Identity.CosmosDb.csproj -c Release --no-build --no-restore -o ./nupkg
      - name: Pack templates
        run: dotnet pack AspNetCore.Identity.Razor.PassKeyPage/AspNetCore.Identity.Razor.PassKeyPage.csproj -c Release --no-build --no-restore -o ./nupkg
      - name: Pack demo project template
        run: dotnet pack AspNetCore.Identity.CosmosDb.Demo.Template/AspNetCore.Identity.CosmosDb.Demo.Template.csproj -c Release --no-build --no-restore -o ./nupkg
      - name: Publish main library package
        run: dotnet nuget push "${{ github.workspace }}/nupkg/AspNetCore.Identity.CosmosDb.*.nupkg" --api-key ${{ secrets.NUGET_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
      - name: Publish passkey template package
        run: dotnet nuget push "${{ github.workspace }}/nupkg/AspNetCore.Identity.CosmosDb.Templates.*.nupkg" --api-key ${{ secrets.NUGET_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
      - name: Publish demo app template package
        run: dotnet nuget push "${{ github.workspace }}/nupkg/AspNetCore.Identity.CosmosDb.Demo.Template.*.nupkg" --api-key ${{ secrets.NUGET_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
      - name: Publish NuGet symbols
        run: dotnet nuget push "${{ github.workspace }}/nupkg/*.snupkg" --api-key ${{ secrets.NUGET_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
      - name: Upload package artifacts
        uses: actions/upload-artifact@v4
        with:
          name: nuget-packages
          path: |
            nupkg/*.nupkg
            nupkg/*.snupkg

  create-release:
    name: Create GitHub Release
    needs: publish
    runs-on: ubuntu-latest
    if: needs.publish.outputs.is_stable_tag == 'true'
    steps:
      - name: Download package artifacts
        uses: actions/download-artifact@v4
        with:
          name: nuget-packages
          path: nupkg

      - name: Create GitHub release with packages
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.publish.outputs.release_tag }}
          generate_release_notes: true
          prerelease: false
          files: |
            nupkg/*.nupkg
            nupkg/*.snupkg

